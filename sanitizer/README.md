How to get good output.html for testing in `sanitize` dir:

1. Open series page in a browser
2. Click "Show All", (chosen series has more than 20 books)
3. Open devtools in elements view
4. Copy html tag to clipboard
5. Paste to text editor
6. Save as `input.html`
7. `cargo run` will produce `output.html`
